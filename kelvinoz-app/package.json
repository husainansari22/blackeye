{
  "name": "kelvinoz-photo-feed",
  "version": "1.0.0",
  "private": true,
  "description": "Upload a photo on the website and view it as a zoomable phone feed",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node -e \"console.log('No build step required')\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.2"
  }
}
